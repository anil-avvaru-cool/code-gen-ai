

## Architecture diagram
Make sure no syntax errors and generate mermaid architecture diagram in doc folder

## Project structure
Generate Project structure, Make sure no mermaid syntax errors, save in doc

## Screen mapping
Generate Mapping of screens or modules, Make sure no mermaid syntax errors, save in doc

## Test scenarios
Make sure no mermaid syntax errors and generate test scenarios list, save in doc

## User story
Create detailed User story with clear description, acceptance criteria, save in doc, use following test scenario: "Create Issue via CreateIssueAgent: verifies IssueForm fields saved, IssueID generated, optional notification sent when NotifyOnCreate=1."

## User story Gherkin format
Create detailed User story with Gherkin format, clear description, acceptance criteria, save in doc, use following test scenario: "Create Issue via PostIssue (HTTP context): ensures request items mapped to Title/Description/Priority, Status defaults to New, response JSON returned"

## ER prompt
Make sure no mermaid syntax errors and generate diagram similar to entity relationship diagram, save in doc

## ER Prompt, enhanced
Perform following steps.
- Make sure no mermaid syntax errors
- Consider new forms AuditLog.dxl and commentForm.dxl
- Generate new diagram similar to entity relationship diagram
- Save in doc